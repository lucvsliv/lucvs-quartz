##### yargs package 설치
```shell
npm install --save-dev @types/yargs
```

##### local server 실행
- Default: port 8080
```shell
npx quartz build --serve
```

- explicit port
```shell
npx quartz build --serve --port 5555
```

##### 실행 화면
![[images/스크린샷 2025-04-04 14.45.30.png]]